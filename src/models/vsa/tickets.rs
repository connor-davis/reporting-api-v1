use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};

#[derive(Debug, Deserialize, Serialize)]
pub struct VsaTicket {
    pub id: i32,
    pub partition_id: String,
    pub asset_agent_id: String,
    pub asset_machine_group_name: String,
    pub ticket_id: String,
    pub ticket_number: String,
    pub service_desk: String,
    pub summary: String,
    pub status: String,
    pub stage: String,
    pub priority: String,
    pub severity: String,
    pub category: String,
    pub resolution: String,
    pub resolution_description: String,
    pub submitter_type: String,
    pub submitter_name: String,
    pub submitter_email: String,
    pub submitter_phone: String,
    pub contact_name: String,
    pub contact_email: String,
    pub contact_phone: String,
    pub assignee: String,
    pub owner: String,
    pub organization: String,
    pub creation_date_time: DateTime<Utc>,
    pub modification_date_time: DateTime<Utc>,
    pub closed_date_time: DateTime<Utc>,
    pub due_date: DateTime<Utc>,
    pub projected_date: DateTime<Utc>,
    pub locked_by: String,
    pub locked_on_date_time: DateTime<Utc>,
    pub source_type: String,
    pub last_public_update_date: DateTime<Utc>,
    pub resolution_date: DateTime<Utc>,
    pub policy: String,
    pub description: String,
    pub is_archived: String,
}
